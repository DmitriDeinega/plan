[project]
name = "plan-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pymongo>=4.6",
  "python-dotenv>=1.0",
  "pydantic>=2.6",
  "tzdata>=2024.1",
]

[tool.uvicorn]
factory = false